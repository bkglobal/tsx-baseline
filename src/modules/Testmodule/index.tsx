
import { connect } from 'react-redux';
import Testmodule from './Testmodule';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(Testmodule);
